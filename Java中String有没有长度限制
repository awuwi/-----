String源码中有个计数的count是int型，按理说应该可以存2^31 = 2147483648个字符，即2G的字符。
但是事实上并非如此，在《深入理解java虚拟机》一书中，String类型的变量的值在常量池中使用两个字节的无符号二进制位表示，
因而最大长度应该是2^16 = 65535.
有趣的是：
65535只是在编译阶段的限制，运行中时，String对象的内部字符数组可以通过其它手段拼接成大于65535长度的字符串。
所以运行时的字符串长度最多还应该是Integer.MAX_VALUE。
